# build output
dist/
.astro/

# wrangler local dev state (miniflare SQLite cache/observability state from
# `wrangler dev` / scripts/verify-security-headers.mjs) - local emulation
# state, not source.
.wrangler/

# dependencies
node_modules/

# env / local secrets - never commit real secrets
.env
.env.*
!.env.example

# Local dev stand-in for the CI approval manifest, regenerated on every
# `npm run build` / `npm test` by scripts/generate-approval-manifest.mjs - see
# src/lib/approval.ts. Contains git commit SHAs that go stale on the next commit,
# so it's a build artifact, not source.
approval-manifest.local.json

# editors / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# test artifacts
coverage/
test-results/
playwright-report/

# Local/CI-run audit output from scripts/ci/run-scheduled-publish.mjs and
# scripts/verify-preview-performance.mjs - generated fresh every run
# (uploaded as a real workflow artifact in CI), never source.
schedule-audit.json
preview-performance.json
