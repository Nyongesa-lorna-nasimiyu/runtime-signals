{
  // Deployment configuration only - not deployed by this checkpoint. Validate locally
  // with `npm run wrangler:dry-run` (wrangler deploy --dry-run), which never publishes.
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "runtime-signals",
  "compatibility_date": "2026-08-28",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"
  }
}
